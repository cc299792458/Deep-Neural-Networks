"""
    This is the implementation of a deep fully convolutional network used for 
"""

import os
import torch
import torch.nn as nn
import torch.optim as optim

class FCN(nn.Module):
    def __init__(self):
        pass